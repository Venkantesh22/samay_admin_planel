import 'package:flutter/material.dart';

class UserInfoSideBar extends StatelessWidget {
  const UserInfoSideBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
